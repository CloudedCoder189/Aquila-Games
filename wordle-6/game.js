const answers = "PLANET STREAM BRIDGE CAMERA SCHOOL BRIGHT FRIEND GARDEN SILVER ORANGE PURPLE YELLOW WINTER SUMMER SPRING AUTUMN FOREST MARKET PLAYER WRITER EDITOR REPORT SOURCE BYLINE COLUMN LAYOUT DESIGN ONLINE SCREEN BUTTON PUZZLE LETTER ANSWER RANDOM CHOICE RESULT FUTURE MEMORY PEOPLE PERSON FAMILY PARENT SISTER NUMBER LITTLE ALWAYS SHOULD BEFORE REALLY PUBLIC CHANGE AROUND DURING OFFICE STREET TRAVEL FLIGHT DRIVER ENGINE ENERGY NATURE ANIMAL RABBIT TURTLE MONKEY BOTTLE COFFEE COOKIE DINNER CEREAL CHEESE BUTTER BANANA CHERRY TOMATO CARROT PEPPER GARLIC POTATO FLOWER BRANCH LEAVES GROUND ISLAND DESERT OCEANS VALLEY CANYON CLOUDS BREEZE SHADOW SUNSET GOLDEN SECRET HIDDEN HONEST SIMPLE CLEVER STRONG GENTLE ACTIVE FAMOUS UNIQUE MODERN USEFUL CAREER CAMPUS LESSON COURSE SPORTS SOCCER TENNIS RUNNER JUMPER BASKET RACING TROPHY MEDALS ROBOTS CODING PYTHON SERVER BINARY SEARCH CREATE LAUNCH UPDATE SYSTEM DEVICE MOBILE LAPTOP WINDOW FOLDER CLOUDY SAFETY SECURE VERIFY DETAIL FORMAT READER POSTER SPEECH DEBATE ACTING SCRIPT GUITAR PIANOS ARTIST PAINTS MOVIES CINEMA COMEDY DRAMAS SCENES DIRECT HIKING SURFER PICNIC VACATE HOTELS TICKET BUDGET SAVING WALLET DOCTOR NURSES HEALTH SLEEPY WALKER JOGGER LEADER MEMBER GROUPS CLUBBY SOCIAL CHATTY SMILEY DREAMS EFFORT SKILLS TALENT IMPACT WINNER FINISH STARTS MIDDLE ENDING WEEKLY YEARLY MINUTE SECOND LATELY ALMOST ENOUGH BETTER CHANCE REASON WONDER METHOD SELECT DELETE RETURN BACKUP WHITES BLACKS CENTER SQUARE CIRCLE BORDER SPACES TABLET HEIGHT SCROLL SMOOTH POLICY PLEASE RIGHTS UNITED POSTED WITHIN STATES PRICES ACCESS RATING AUTHOR PHOTOS THREAD ACTION SERIES THINGS RECENT STORES AUGUST SINGLE STATUS BROWSE SELLER BECOME REGION RECORD TOPICS VIDEOS SUBMIT AMOUNT THOUGH THANKS WEIGHT POINTS BEAUTY ANNUAL AGENCY ACROSS SEASON VOLUME ANYONE RATHER SUPPLY ADVICE RENTAL TAKING VALUES SAMPLE PHONES POLICE WANTED SURVEY SIMPLY TRYING SENIOR BEHIND ALLOWS BOARDS STRING UNLESS TARGET BRANDS PLACES PRETTY AWARDS STUDIO REMOTE THEORY VISUAL AGENTS REPAIR SECTOR QUOTES BEYOND BOTTOM SWITCH TITLES VISION ACCEPT APPEAR ACTUAL REDUCE POCKET SIGNED WORKED SORTED BECAME SAYING SPIRIT TABLES PLANTS AVENUE VIEWED ATTACK PLACED PLAYED SHIRTS PROFIT SENATE TURNED SIGNAL STATED ADULTS SERVED BOUGHT BROKEN BATTLE SHARED SOUNDS REMAIN ASSETS PORTAL TOWARD ASSIST POSTAL BRAZIL STATIC VENDOR AGREED SCHEME TURKEY PROPER SHARES VOYEUR APPEAL ADDING PRINTS SUITES SERIAL RELIEF RECIPE PERMIT SEEING BUREAU PIECES STRESS TRENDS POETRY SPREAD AFFECT VIRGIN RAISED BLONDE ALBUMS AGENDA ANYWAY TRACKS PRINCE SYMBOL STYLES SCORES WEBLOG BEGINS REFORM ALERTS ASSUME SAFARI TESTED SHOWED POTTER STOCKS BEHALF SALARY RESUME TWENTY AVATAR ATTEND SHOWER SEEMED ALUMNI THEMES POWERS ASKING BLOCKS BODIES SHEETS PLASMA BANNER STANDS WHEELS ROUTER WOMENS UPLOAD VOTING REGARD STRIKE THREAT STORED STABLE SEVERE VISITS BERLIN POUNDS RUBBER BRINGS STEREO STAMPS THONGS PRAYER APACHE TAUGHT WORKER TEMPLE BREAKS WATERS PREFER VECTOR SHAVED BUFFER SYNTAX PRISON SIERRA SUMMIT ARCADE TRIALS TISSUE ASPECT PRICED RIDING RESCUE ABROAD SERVES PLENTY THROAT WEALTH VACUUM WRITES PLATES STUPID RISING REPEAT PUPILS POLISH TROOPS BROKER TRUCKS SENSOR ANGELS SEALED WOODEN SHORTS REFUND WIDELY PHRASE BIGGER VERSUS BABIES SLOWLY UNLIKE WRIGHT PROVEN WARREN WEBCAM SOCKET SILENT ANALOG SEEKER WISDOM STAGES POWDER ASSESS STONES TRIPLE TITANS SOUGHT BASICS WARNER WIZARD ACTORS PICKED BELIEF BIKINI REFINE BIDDER SINGER PLUGIN TERROR THIRTY REFERS VICTIM ARRIVE SLEEVE STROKE SKIING TIMING RIVERS THUMBS TWELVE VOICES WALKED AFRAID TAGGED BISHOP PULLED SHAPED SPEARS WORLDS THESIS PIXELS TOTALS AFFORD TURNER SPOKEN STAYED ROGERS REGIME WISHES BREATH SOLELY VOTERS THINKS TRICKS TONGUE ATOMIC APPROX QUOTED BRONZE SENDER SPOUSE VIEWER PROVED SALMON WEAPON BURDEN REALTY TOILET RANKED ROUTES TIMELY TALKED VILLAS BROOKS WHILST PROMPT VICTOR ATTACH SPIDER RANGES TRAILS VENUES SHIELD PICKUP SACRED SCORED BELONG BARNES UNIONS SCALES STRAIN ADJUST SOVIET TREATY RADIUS TENDER PRAISE ABSENT BUBBLE VESSEL RELATE SUFFER RETAIN TUNNEL BEAVER ANCHOR SURELY STYLUS STRUCK BRIDAL TRIBAL REBATE TWINKS TECHNO RARELY TRAINS ADVISE BOXING REVEAL STRICT TIMBER RULING STEADY ASSURE SODIUM TRIVIA RIBBON TRADES SUPERB SPEAKS BURTON ROLLER ASTHMA SPRINT BACKED SLIGHT ARREST PRIZES PURSUE PLAINS RACIAL SAFELY ALLIED THROWS ROSTER TUNING ROCKET BULLET TOWERS PRIEST TRANCE BUNDLE ARCTIC STOLEN AGREES ZONING BURIED ROBUST PORTER ALPINE SHAPES BREEDS RAPIDS BAILEY VARIED ASSIGN TIGERS AURORA SLIDES RHYTHM ARGUED SUDDEN SPEEDS VOCALS BURNER WORTHY SAINTS QUEENS TRIBES TENANT TATTOO REMEDY BARELY SURREY SOLVED POSING URGENT BORING SCHEMA PREFIX BARREL TYPING AERIAL WICKED PUSHED REGGAE PLANES TACKLE BUILDS STICKS STRAND REJECT VALUED BATMAN SETTLE SCENIC SEWING TRUSTS PILLOW ROLLED TRADER THROWN RAISES BALLOT SQUIRT BEINGS ACCENT AUBURN UNLOCK PLEDGE RIDERS REMARK VARIES PIERCE BREACH WIRING PHASES BALLET BUMPER BANNED BRIEFS RADIOS TARIFF PETITE STRIPS ROTARY SKIRTS ROUNDS BAKING BRAKES STICKY BRUTAL YIELDS SUITED VERTEX WAIVER VALVES VELVET BLOODY AFFAIR WASHER BESIDE TITLED SPHERE RATIOS WALNUT BLADES TRAUMA ADVERT SLEEPS VIOLIN WARNED APOLLO VERBAL RENDER SAVAGE WILLOW SUBTLE VIRTUE SHADES ADWARE VIKING BOTHER RESIST RANGER SCARED ASYLUM POISON ASLEEP REGRET BURNED SCREAM BORROW BEHAVE ABOARD BEATEN RECKON ROTTEN ROBBED AUNTIE RIPPED SUPPER WASHED PUNISH BITTER THRONE ADMIRE STATUE BUCKET WAITER BOOKED BETRAY TEMPER TRAGIC UNFAIR POLITE ABSURD BURGER BARNEY SKINNY SHITTY WOLVES SORROW PSYCHO UPSIDE SCOTCH SUCKER RITUAL WARDEN ZOMBIE SAILOR BRANDY PISTOL SUBWAY ANYHOW PLAGUE RASCAL PHOEBE TRICKY PIRATE RENTED RANSOM SHEILA SHRINK ARCHER RUNWAY SHRIMP BEGGED BOUNCE VIOLET BEWARE RACIST UPBEAT TUCKER POPPED SALUTE TANNER AWHILE STARVE ACCUSE SKETCH BEHOLD PUPPET ADDICT WANDER SERENA BUGGER PIGEON THRILL TASTED SADDLE BURIAL BOUNTY AMBUSH AMAZED AVENGE RACKET SNIPER VOYAGE BANDIT SHELLY SULTAN PETROL SHOVEL WARMTH SHERRY BEGGAR BUNKER ROOKIE BARBER BOMBER SAVIOR SWEATY BANKER SLOPPY SQUEAL REFUGE THIRST SPLASH SUMMON VANISH BITING TEXTED SHRINE ROBBER TIPPED SEDUCE POTION STROLL STITCH THRUST ARTERY BENDER WARMER ASHORE BEACON SHAKEN VULGAR BAKERY SNATCH WEAVER WEIRDO PICKLE TICKLE STREAK RAPIST SHORTY SPONGE RESENT RIDDLE BOILED SNITCH BUCKLE VANITY YOGURT TAILOR BLOUSE REGAIN WEIGHS TELLER RAGING UNLOAD STINKY THIGHS RIGGED SPOOKY BEETLE SNEAKY TAPPED BUFFET PINNED SINNER PERISH WRETCH WEASEL PREACH VOODOO RATTLE BAMBOO SPINAL BUMMER RECESS SQUASH SALOON SAWYER TAVERN SMELLY TRENCH BATTER BOOGIE QUARRY UTMOST BRUNCH SCRAPE SHAGGY REPENT VACANT PURITY SERMON WHOOSH SPIRAL BRUISE RIPPER SELDOM POETIC WRENCH SOLEMN BLIMEY BIRDIE REVOLT ZIPPER BOILER UNEASY STENCH SQUIRE ABSORB TYRANT SINNED REVIVE STANCE BARKER SLEIGH WEDDED THINGY SHAMAN BARREN SIGNOR BADGER UPTOWN VIABLE SLOGAN SLAYER UNREAL UNPACK VITALS ANTHEM SALIVA WIGGLE AWAKEN REFILL SNEEZE PILLAR SAUCER RUBBLE WARMED SHOGUN WRAITH REAPER SPEEDY RUBBED AMULET TINKER PLUNGE SLICED THRIVE SCARCE UNWELL STUFFY".split(" ")

const rows = 6
const columns = 6
const keyboardRows = ["QWERTYUIOP", "ASDFGHJKL", "ZXCVBNM"]
const storageKey = "aquila-wordle-unlimited-6"
answers.forEach((word) => window.VALID_WORDS.add(word))
const statePriority = { empty: 0, absent: 1, present: 2, correct: 3 }
let answer = ""
let usedAnswers = []

let game = {
  guesses: [],
  current: "",
  finished: false,
  won: false
}

try {
  const saved = JSON.parse(localStorage.getItem(storageKey))
  if (saved && Array.isArray(saved.guesses) && typeof saved.answer === "string" && saved.answer.length === columns) {
    game = {
      guesses: saved.guesses,
      current: saved.current || "",
      finished: Boolean(saved.finished),
      won: Boolean(saved.won)
    }
    answer = saved.answer
    usedAnswers = Array.isArray(saved.usedAnswers) ? saved.usedAnswers.filter((word) => answers.includes(word)) : []
  }
} catch {
  localStorage.removeItem(storageKey)
}

function pickAnswer() {
  let available = answers.filter((word) => !usedAnswers.includes(word))
  if (available.length === 0) {
    usedAnswers = []
    available = [...answers]
  }
  const next = available[Math.floor(Math.random() * available.length)]
  usedAnswers.push(next)
  return next.toUpperCase()
}

if (!answer) answer = pickAnswer()
else if (!usedAnswers.includes(answer)) usedAnswers.push(answer)

const shell = document.querySelector(".game-shell")
const board = document.querySelector("#board")
const keyboard = document.querySelector("#keyboard")
const toast = document.querySelector("#toast")
const helpDialog = document.querySelector("#help-dialog")
const resultDialog = document.querySelector("#result-dialog")
const viewResult = document.querySelector("#view-result")
const newPuzzleButton = document.querySelector("#new-puzzle-button")

if (new URLSearchParams(location.search).get("embed") === "1") {
  shell.classList.add("embed")
  document.documentElement.classList.add("embed-page")
  document.body.classList.add("embed-page")
}

function scoreGuess(guess) {
  const result = Array(columns).fill("absent")
  const remaining = answer.split("")

  for (let index = 0; index < columns; index += 1) {
    if (guess[index] === answer[index]) {
      result[index] = "correct"
      remaining[index] = ""
    }
  }

  for (let index = 0; index < columns; index += 1) {
    if (result[index] === "correct") continue
    const match = remaining.indexOf(guess[index])
    if (match >= 0) {
      result[index] = "present"
      remaining[match] = ""
    }
  }

  return result
}

function saveGame() {
  localStorage.setItem(storageKey, JSON.stringify({ ...game, answer, usedAnswers }))
}

function showToast(message) {
  toast.textContent = message
  toast.classList.add("show")
  clearTimeout(showToast.timer)
  showToast.timer = setTimeout(() => toast.classList.remove("show"), 1800)
}

function getLetterStates() {
  const states = {}
  game.guesses.forEach((guess) => {
    scoreGuess(guess).forEach((state, index) => {
      const letter = guess[index]
      const existing = states[letter] || "empty"
      if (statePriority[state] > statePriority[existing]) states[letter] = state
    })
  })
  return states
}

function renderBoard() {
  board.innerHTML = ""
  for (let rowIndex = 0; rowIndex < rows; rowIndex += 1) {
    const row = document.createElement("div")
    row.className = "board-row"
    row.dataset.row = rowIndex
    const submitted = game.guesses[rowIndex]
    const active = rowIndex === game.guesses.length && !game.finished
    const letters = submitted || (active ? game.current : "")
    const scores = submitted ? scoreGuess(submitted) : []

    for (let columnIndex = 0; columnIndex < columns; columnIndex += 1) {
      const tile = document.createElement("div")
      const letter = letters[columnIndex] || ""
      const state = scores[columnIndex] || "empty"
      tile.className = `tile ${state}${letter && !submitted ? " filled" : ""}`
      tile.textContent = letter
      tile.setAttribute("aria-label", submitted ? `${letter}, ${state}` : letter || "empty")
      row.appendChild(tile)
    }
    board.appendChild(row)
  }
}

function renderKeyboard() {
  keyboard.innerHTML = ""
  const letterStates = getLetterStates()
  keyboardRows.forEach((letters, rowIndex) => {
    const row = document.createElement("div")
    row.className = "keyboard-row"
    if (rowIndex === 2) row.appendChild(createKey("ENTER", "key-wide"))
    letters.split("").forEach((letter) => row.appendChild(createKey(letter, letterStates[letter] || "")))
    if (rowIndex === 2) row.appendChild(createKey("⌫", "key-wide", "BACKSPACE", "Delete letter"))
    keyboard.appendChild(row)
  })
}

function createKey(label, className, value = label, ariaLabel = label) {
  const button = document.createElement("button")
  button.className = `key ${className}`
  button.type = "button"
  button.textContent = label
  button.setAttribute("aria-label", ariaLabel)
  button.addEventListener("click", () => handleKey(value))
  return button
}

function submitGuess() {
  if (game.finished) return
  if (game.current.length !== columns) {
    const activeRow = board.querySelector(`[data-row="${game.guesses.length}"]`)
    activeRow.classList.add("shake")
    setTimeout(() => activeRow.classList.remove("shake"), 420)
    showToast("Not enough letters")
    return
  }

  if (!window.VALID_WORDS.has(game.current)) {
    const activeRow = board.querySelector(`[data-row="${game.guesses.length}"]`)
    activeRow.classList.add("shake")
    setTimeout(() => activeRow.classList.remove("shake"), 420)
    showToast("Not in word list")
    return
  }

  game.guesses.push(game.current)
  game.won = game.current === answer
  game.finished = game.won || game.guesses.length === rows
  game.current = ""
  saveGame()
  render()

  const latestRow = board.querySelector(`[data-row="${game.guesses.length - 1}"]`)
  latestRow.querySelectorAll(".tile").forEach((tile, index) => {
    tile.classList.add("reveal")
    tile.style.animationDelay = `${index * 120}ms`
  })

  if (game.finished) setTimeout(openResult, 1450)
}

function handleKey(key) {
  if (game.finished) return
  if (key === "ENTER") return submitGuess()
  if (key === "BACKSPACE") game.current = game.current.slice(0, -1)
  else if (/^[A-Z]$/.test(key) && game.current.length < columns) game.current += key
  saveGame()
  renderBoard()
}

function openResult() {
  document.querySelector("#result-kicker").textContent = "SIX-LETTER WORDLE"
  document.querySelector("#result-title").textContent = game.won ? "Nicely reported." : "That was a tough one."
  document.querySelector("#result-description").innerHTML = game.won
    ? `You found it in ${game.guesses.length} ${game.guesses.length === 1 ? "guess" : "guesses"}.`
    : `Today's word was <strong>${answer}</strong>.`
  const miniGrid = document.querySelector("#mini-grid")
  miniGrid.innerHTML = ""
  game.guesses.forEach((guess) => {
    const row = document.createElement("div")
    row.className = "mini-row"
    scoreGuess(guess).forEach((state) => {
      const tile = document.createElement("span")
      tile.className = `mini-tile ${state}`
      row.appendChild(tile)
    })
    miniGrid.appendChild(row)
  })
  resultDialog.showModal()
}

function startNewPuzzle() {
  answer = pickAnswer()
  game = { guesses: [], current: "", finished: false, won: false }
  saveGame()
  resultDialog.close()
  render()
  showToast("New puzzle ready")
}

function render() {
  renderBoard()
  renderKeyboard()
  viewResult.hidden = !game.finished
}

document.addEventListener("keydown", (event) => {
  const key = event.key.toUpperCase()
  if (key === "ENTER" || key === "BACKSPACE" || /^[A-Z]$/.test(key)) {
    event.preventDefault()
    handleKey(key)
  }
})

document.querySelector("#help-button").addEventListener("click", () => helpDialog.showModal())
document.querySelectorAll(".modal-close").forEach((button) => button.addEventListener("click", () => button.closest("dialog").close()))
viewResult.addEventListener("click", openResult)
newPuzzleButton.addEventListener("click", startNewPuzzle)

saveGame()
render()
