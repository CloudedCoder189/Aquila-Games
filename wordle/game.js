const answers = "PRESS STORY WRITE MEDIA QUOTE DEBUG SOUND AGILE STEEL FORCE BLUNT CIVIC EXCEL TREND BLADE BLAME AUDIO POUND COULD CABIN ADMIT TODAY VISIT WHICH BASIC WOULD AWAIT DRAFT ELECT CHALK CREAM ERROR BUILD SIGHT QUIET ENJOY SHIRT ORDER SHARP KNOWN WASTE RANGE CATER LUCKY ANGRY SPORT CLICK PRIZE FEAST ESSAY HONEY WRONG FLASH CLONE PHONE EATEN EMBED CHILD LARGE CRUSH ROUND ABOUT EMAIL OCEAN BRIEF FAULT CARRY MOTOR ROYAL DANCE BOOST BRINK CHEER LOCAL CREEK PRIDE GIANT AMONG CLIMB BLAST ARMOR CURVE CRAZE IMAGE TOWER FULLY ELITE AMEND CHAOS BADGE CLASS TOUGH TOPIC NOISE TRUTH FOCUS CHIME MATCH NIGHT BOOTH NEVER ALIKE CHASE MUSIC CLOCK ANKLE YOUTH POWER ROUGH STILL ELBOW FROST ISSUE CLEAR BRICK OTHER CATCH SENSE CLAIM EVENT BRAVE INPUT ENACT PLAIN EAGLE FINAL GUARD DIARY DROWN NURSE CROSS CANAL FRONT BLANK ASIDE METAL GLASS FETCH STAGE DWELL ARISE STAFF DELAY BURST FIELD CRAVE DOZEN BLAND MOUSE HUMAN DRIVE BLOWN SPEND BROOK CEASE WORRY PAPER BUYER BEACH FIERY ROUTE ARROW BELOW HAPPY ALTER ALLOW SPEAK THEME CRATE SCENE AMBER CHIEF BEARD SHOWN BEGAN PHOTO TREAT DITCH TOTAL BERRY BASIN COMIC PROUD ANGLE GRACE DAILY FAITH BEAST MODEL ATONE SHOCK NORTH FLOOR ANNOY GUIDE WHERE SHORT CHEEK BRIDE CRAFT THERE RAISE ARRAY BATHE TABLE DRIED BOAST TRIAL PLACE DUSTY FLAME APPLY IDEAL BRAKE AGENT AMAZE SHARE HEAVY DRIFT FLOAT AFTER PLANT CACHE DEPTH SHIFT BLINK READY COACH AUDIT FLESH SUGAR GRAND POINT BLEND RIGHT THANK TRADE FRUIT PIECE FAVOR CHART EQUAL YOUNG COURT FAINT PROVE STORE HOUSE FLEET MAJOR GIVEN DELTA STEAM CHECK WATCH BLOOD ALIVE WHOLE DAISY APART EVERY QUICK DRAWN CANOE CRISP BATCH AHEAD CRAZY SPACE CHOIR SCALE MONTH HEART TRAIN WATER BAKER DEALT CRAWL DECOR BRAND ADAPT CYCLE ALBUM GROUP CHAIR DOUGH EARLY DOUBT MIGHT RIVER ALONG BRACE CARGO DROVE DOING AMPLE BROKE GUESS MOUNT VIDEO THEIR THESE PRICE STATE WORLD BOOKS YEARS TERMS THOSE USING BASED BLACK BEING WOMEN SOUTH WHILE THREE THINK POSTS SINCE BOARD WHITE SMALL TIMES SITES TITLE SHALL TOOLS REPLY VALUE PRINT STOCK SALES START STUDY AGAIN USERS ABOVE RATES POKER AREAS ADDED RADIO UNTIL TRACK STYLE WORKS RULES THING THIRD WORDS TAKEN SHOWS SPEED STUFF SHOES VIEWS PLANS TYPES ASKED WEEKS UNION TEENS WOMAN SCORE SUPER ROOMS VOICE BROWN SMITH PRIOR BUILT BASIS AWARD PETER RATED QUITE STARS TAKES BRING VALID WROTE SHIPS SEEMS TRUST BREAK BLOCK SONGS WORTH AGREE BLOGS STAND BEGIN BIBLE SUITE SHEET SEVEN WHOSE STONE TESTS PHASE ALONE SAINT UPPER PRIME STEPS SHOPS URBAN TOURS ADMIN SOLID TOUCH SERVE SMART AVOID BIRTH VIRUS ABUSE REACH SORRY TEAMS TRIED TAXES BRAIN STATS WANTS ALERT QUEEN SWEET TRUCK VOTES SIGNS ANIME QUERY RURAL BANKS WALES SPENT SLEEP RINGS ALPHA BONUS TREES REFER BABES RATIO PROOF SPECS BOXES AWARE SHAPE USAGE WHEEL ANGEL WIDTH PLATE SIZES TRAIL BUDDY SETUP BLUES SCOPE BEARS STICK SLIDE BOUND STORM PILOT ULTRA PLAYS TRULY BROAD BANDS POLLS YOURS SHELL SOLAR TASKS SPLIT TWICE BIRDS SAVED SHOTS PIANO RISKS PORTS TEACH RAPID BOOTS PULSE STRIP BILLS ASSET TIGHT PLANE VILLA ROMAN TASTE TRIPS SIDES TURNS SEATS ALARM USUAL VINYL WORST PICKS SMOKE BLIND ACTOR WINGS RIDGE SKILL THROW WORSE BOATS TELLS TALKS BONDS ROGER SPRAY ROADS YIELD RADAR WALLS PIZZA YARDS WOODS BRASS PLAZA QUEST TRANS BOOTY ACRES VENUE VITAL WELLS THICK VISTA TERRY ARENA SMILE ARMED PILLS TIGER BALLS ROSES BREAD BILLY SOLVE TONES TOWNS ROLES RUGBY SIZED THEFT SWING SHOOT POEMS ROBOT WINDS ROOTS SHORE SLOTS ROCKS ATLAS TEXTS AGING SIGMA TRACE SPOKE WEIRD ROBIN STRAP VALVE SHELF ADOPT TALES SEEDS TIRED ACUTE STOOD STACK TRUNK WAVES SAUCE BEADS PROXY VOTED BIKES SLAVE RANCH TONER ALIEN ROCKY BUNCH SAVER WANNA SEEKS SPOTS SALON TURBO THATS AIMED BRUSH SPARE SKIRT SIXTH SANDY PITCH AUTOS STAMP TAPES ZONES RACES PROBE BINGO SUNNY BURNS PUMPS RELAY TEARS ROLLS RALLY STUCK STOPS VOCAL TOXIC BENCH RIDER BOBBY SHEEP WINES SALAD RELAX SWORD SELLS PIXEL ACIDS SQUAD WAGES WHEAT BASES UNITY BEGUN SOCKS ROVER TANKS SPELL ANNEX WIRED ARGUE AMINO TRIES TRICK WIDER SCREW UNCLE RANDY TIRES RETRO ANGER SUITS TRIBE TWINS WALKS SUCKS BONES BREED POLAR BEANS SNAKE SENDS TIMER VERSE RACKS TUMOR WATTS TUBES QUEUE SKINS WELSH BELLY SONIC THUMB TWIST RANKS REMIX ALIAS SPICE TRASH SHADE POOLS SAVES PUNCH SHAKE SILLY SHAME SHEER WITCH PUPPY SMELL SATIN PROMO TUNES RENEW REBEL SLOPE WHATS RIDES REHAB SHAFT BEATS SCUBA STEAL TUNER SCOUT BUCKS WRIST REALM BUSES ROUGE YEAST WIVES SORTS VIRAL PIPES SALLY YACHT WHALE SHARK TRACT SHINE SERUM SWIFT SAMBA WOUND BELLE BOXED SPIES BUNNY BATHS TOKEN BELTS BURKE TEDDY RULED SCARY TOOTH STAYS UPSET PURSE ALIGN BLESS ALLOY PLOTS BLOOM SURGE SOULS SPANK VAULT WIRES BACON SPINE TROUT BADLY SCOOP TILES AWFUL WAGON QUILT BLOND BORED SAFER THETA STAKE ARBOR SIGHS SWEAR STOLE THREW SLEPT THIEF BLEEP SCARE AWAKE TOAST BRAVO SIREN SHOUT SWEAT SKULL SNEAK ALLEY ALIBI WIDOW PRICK BURNT TENSE STEAK RIFLE SONNY BOOZE VODKA SMASH SOBER TOWEL BARON PETTY SARGE SPOIL BLOKE BENNY WRECK STINK STARE SHAVE REACT SWELL ROMEO RUSTY SHOVE RUMOR SPIKE BLEED SWEEP RISKY SLICE STIFF WOODY SCENT SWORE ROAST BULLY SPILL SNACK SPITE SWORN TASTY STARK PIPER WILLY SPOON SWAMP TORCH STALL SMACK STUNT SHINY BRIBE STRAW WEIGH PLEAD SEIZE STING ATTIC SCARF ROGUE RIVAL SPARK ADORE STOVE RAZOR WALLY PRANK ALTAR STAIN TEASE BAKED PHONY WHACK WAIST URINE PINCH PORCH TAMMY SUSHI SHOOK SWINE REIGN STASH SWEPT VOMIT AISLE RAVEN QUEER TRUMP SPICY STEER UNITE STRAY RABBI TRAMP STEED PIGGY SCRAP PSYCH SENOR AGONY SHACK SCRUB RULER UTTER TANGO SPEAR BOXER YUMMY SLACK BRENT SHIVA POPPY WALTZ BLISS WRATH TUTOR STERN SEWER BLUFF TROOP BROOM SIXTY BARGE SYRUP SNIFF SLICK VAGUE TENTH TYING TAKIN RHYME ABORT TUMMY TRUCE VERGE SCRAM BRUTE TELLY ABBEY WEARY STOOL SKATE VICAR PUPIL SALTY RODEO SHADY RAINY BASIL SIEGE AMIGO VENOM SHUSH TREAD WAGER THIGH APRON TONIC STEEP QUACK TROLL SQUID ARSON SLATE SCOLD SQUAT RISEN SLASH THORN VALET STEIN TITAN VOUCH WHOOP SHALT PLUCK SAUNA TEMPT WINDY BOGUS TWEET SKUNK USHER BLAZE SALSA SHRED ABYSS ABIDE THINE VIVID SHAKY BLUSH SNOOP SLIME SONAR POSSE YIKES ADIOS AMUSE RECON THUMP".split(" ")

const rows = 6
const columns = 5
const keyboardRows = ["QWERTYUIOP", "ASDFGHJKL", "ZXCVBNM"]
const storageKey = "aquila-wordle-unlimited-5"
answers.forEach((word) => window.VALID_WORDS.add(word))
const statePriority = { empty: 0, absent: 1, present: 2, correct: 3 }
let answer = ""
let usedAnswers = []

let game = {
  guesses: [],
  current: "",
  finished: false,
  won: false
}

try {
  const saved = JSON.parse(localStorage.getItem(storageKey))
  if (saved && Array.isArray(saved.guesses) && typeof saved.answer === "string" && saved.answer.length === columns) {
    game = {
      guesses: saved.guesses,
      current: saved.current || "",
      finished: Boolean(saved.finished),
      won: Boolean(saved.won)
    }
    answer = saved.answer
    usedAnswers = Array.isArray(saved.usedAnswers) ? saved.usedAnswers.filter((word) => answers.includes(word)) : []
  }
} catch {
  localStorage.removeItem(storageKey)
}

function pickAnswer() {
  let available = answers.filter((word) => !usedAnswers.includes(word))
  if (available.length === 0) {
    usedAnswers = []
    available = [...answers]
  }
  const next = available[Math.floor(Math.random() * available.length)]
  usedAnswers.push(next)
  return next.toUpperCase()
}

if (!answer) answer = pickAnswer()
else if (!usedAnswers.includes(answer)) usedAnswers.push(answer)

const shell = document.querySelector(".game-shell")
const board = document.querySelector("#board")
const keyboard = document.querySelector("#keyboard")
const toast = document.querySelector("#toast")
const helpDialog = document.querySelector("#help-dialog")
const resultDialog = document.querySelector("#result-dialog")
const viewResult = document.querySelector("#view-result")
const newPuzzleButton = document.querySelector("#new-puzzle-button")

if (new URLSearchParams(location.search).get("embed") === "1") {
  shell.classList.add("embed")
  document.documentElement.classList.add("embed-page")
  document.body.classList.add("embed-page")
}

function scoreGuess(guess) {
  const result = Array(columns).fill("absent")
  const remaining = answer.split("")

  for (let index = 0; index < columns; index += 1) {
    if (guess[index] === answer[index]) {
      result[index] = "correct"
      remaining[index] = ""
    }
  }

  for (let index = 0; index < columns; index += 1) {
    if (result[index] === "correct") continue
    const match = remaining.indexOf(guess[index])
    if (match >= 0) {
      result[index] = "present"
      remaining[match] = ""
    }
  }

  return result
}

function saveGame() {
  localStorage.setItem(storageKey, JSON.stringify({ ...game, answer, usedAnswers }))
}

function showToast(message) {
  toast.textContent = message
  toast.classList.add("show")
  clearTimeout(showToast.timer)
  showToast.timer = setTimeout(() => toast.classList.remove("show"), 1800)
}

function getLetterStates() {
  const states = {}
  game.guesses.forEach((guess) => {
    scoreGuess(guess).forEach((state, index) => {
      const letter = guess[index]
      const existing = states[letter] || "empty"
      if (statePriority[state] > statePriority[existing]) states[letter] = state
    })
  })
  return states
}

function renderBoard() {
  board.innerHTML = ""
  for (let rowIndex = 0; rowIndex < rows; rowIndex += 1) {
    const row = document.createElement("div")
    row.className = "board-row"
    row.dataset.row = rowIndex
    const submitted = game.guesses[rowIndex]
    const active = rowIndex === game.guesses.length && !game.finished
    const letters = submitted || (active ? game.current : "")
    const scores = submitted ? scoreGuess(submitted) : []

    for (let columnIndex = 0; columnIndex < columns; columnIndex += 1) {
      const tile = document.createElement("div")
      const letter = letters[columnIndex] || ""
      const state = scores[columnIndex] || "empty"
      tile.className = `tile ${state}${letter && !submitted ? " filled" : ""}`
      tile.textContent = letter
      tile.setAttribute("aria-label", submitted ? `${letter}, ${state}` : letter || "empty")
      row.appendChild(tile)
    }
    board.appendChild(row)
  }
}

function renderKeyboard() {
  keyboard.innerHTML = ""
  const letterStates = getLetterStates()
  keyboardRows.forEach((letters, rowIndex) => {
    const row = document.createElement("div")
    row.className = "keyboard-row"
    if (rowIndex === 2) row.appendChild(createKey("ENTER", "key-wide"))
    letters.split("").forEach((letter) => row.appendChild(createKey(letter, letterStates[letter] || "")))
    if (rowIndex === 2) row.appendChild(createKey("⌫", "key-wide", "BACKSPACE", "Delete letter"))
    keyboard.appendChild(row)
  })
}

function createKey(label, className, value = label, ariaLabel = label) {
  const button = document.createElement("button")
  button.className = `key ${className}`
  button.type = "button"
  button.textContent = label
  button.setAttribute("aria-label", ariaLabel)
  button.addEventListener("click", () => handleKey(value))
  return button
}

function submitGuess() {
  if (game.finished) return
  if (game.current.length !== columns) {
    const activeRow = board.querySelector(`[data-row="${game.guesses.length}"]`)
    activeRow.classList.add("shake")
    setTimeout(() => activeRow.classList.remove("shake"), 420)
    showToast("Not enough letters")
    return
  }

  if (!window.VALID_WORDS.has(game.current)) {
    const activeRow = board.querySelector(`[data-row="${game.guesses.length}"]`)
    activeRow.classList.add("shake")
    setTimeout(() => activeRow.classList.remove("shake"), 420)
    showToast("Not in word list")
    return
  }

  game.guesses.push(game.current)
  game.won = game.current === answer
  game.finished = game.won || game.guesses.length === rows
  game.current = ""
  saveGame()
  render()

  const latestRow = board.querySelector(`[data-row="${game.guesses.length - 1}"]`)
  latestRow.querySelectorAll(".tile").forEach((tile, index) => {
    tile.classList.add("reveal")
    tile.style.animationDelay = `${index * 120}ms`
  })

  if (game.finished) setTimeout(openResult, 1450)
}

function handleKey(key) {
  if (game.finished) return
  if (key === "ENTER") return submitGuess()
  if (key === "BACKSPACE") game.current = game.current.slice(0, -1)
  else if (/^[A-Z]$/.test(key) && game.current.length < columns) game.current += key
  saveGame()
  renderBoard()
}

function openResult() {
  document.querySelector("#result-kicker").textContent = "FIVE-LETTER WORDLE"
  document.querySelector("#result-title").textContent = game.won ? "Nicely reported." : "That was a tough one."
  document.querySelector("#result-description").innerHTML = game.won
    ? `You found it in ${game.guesses.length} ${game.guesses.length === 1 ? "guess" : "guesses"}.`
    : `Today's word was <strong>${answer}</strong>.`
  const miniGrid = document.querySelector("#mini-grid")
  miniGrid.innerHTML = ""
  game.guesses.forEach((guess) => {
    const row = document.createElement("div")
    row.className = "mini-row"
    scoreGuess(guess).forEach((state) => {
      const tile = document.createElement("span")
      tile.className = `mini-tile ${state}`
      row.appendChild(tile)
    })
    miniGrid.appendChild(row)
  })
  resultDialog.showModal()
}

function startNewPuzzle() {
  answer = pickAnswer()
  game = { guesses: [], current: "", finished: false, won: false }
  saveGame()
  resultDialog.close()
  render()
  showToast("New puzzle ready")
}

function render() {
  renderBoard()
  renderKeyboard()
  viewResult.hidden = !game.finished
}

document.addEventListener("keydown", (event) => {
  const key = event.key.toUpperCase()
  if (key === "ENTER" || key === "BACKSPACE" || /^[A-Z]$/.test(key)) {
    event.preventDefault()
    handleKey(key)
  }
})

document.querySelector("#help-button").addEventListener("click", () => helpDialog.showModal())
document.querySelectorAll(".modal-close").forEach((button) => button.addEventListener("click", () => button.closest("dialog").close()))
viewResult.addEventListener("click", openResult)
newPuzzleButton.addEventListener("click", startNewPuzzle)

saveGame()
render()
